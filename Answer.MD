# 台科大資管 Python 考試作答區

---

## 基本資料

**學號：** B11409008
**系級：** 資管系大一  
**姓名：** 陳睿賢

---

## 一、 原始碼分析報告 (Static Analysis) (共8小題，每小題3分，總共24分)

### 1.1 變數與邏輯對照表

| 原始變數名稱 | 代表意義 (Business Logic) | 建議修正名稱 |
|---|---|---|
| Q1 l1 (in load_proc) | l1 是一個暫存變數，用來儲存從檔案中讀取的所有行（每行是一個字串）。 |  建議改為更具描述性的名稱，例如 lines 或 file_lines，以清楚表達它是從檔案中讀取的多行內容。|
| Q2 d2 (in main) | d2 是一個全域變數，儲存所有書籍的資料，格式為一個字典的列表，每個字典包含書名（t）、ISBN（i）和狀態（s）。 |  建議改為更具語意的名稱，例如 books 或 library_data，以清楚表達它是圖書資料的集合。|
| Q3 c_res (in check) | c_res 是一個函式，用來檢查某個 ISBN 是否已存在於 d2（圖書資料）中。 |  建議改為更具描述性的名稱，例如 check_isbn_exists 或 isbn_exists，以清楚表達它的功能。|

### 1.2 壞味道 (Code Smells) 診斷

請列出原程式碼中至少 5 個嚴重的設計問題，並說明為什麼不好：

- **Q1：** . 全域變數 d2 的使用
問題:
d2 是一個全域變數，負責儲存所有圖書資料，並在多個函式中直接操作。這種設計會導致程式的可讀性和可維護性降低，因為任何函式都可以隨意修改它的內容，增加了除錯的難度。
為什麼不好:
全域變數容易導致意外的副作用。
無法清楚追蹤資料的來源和修改過程。
不利於單元測試，因為函式之間存在隱式依賴。

- **Q2：**未使用 with 語法處理檔案
問題:
檔案操作中使用了 open() 和 close()，但未使用 with 語法來自動管理檔案資源。
為什麼不好:
如果程式在檔案操作過程中發生例外，檔案可能無法正確關閉，導致資源洩漏。
with 語法更簡潔且安全，應該優先使用。

- **Q3：**硬編碼的檔案名稱
問題:
檔案名稱 lib_data.txt 是硬編碼在程式中的，無法靈活配置。
為什麼不好:
如果需要更改檔案名稱，必須修改程式碼，違反了開放封閉原則（OCP）。
不利於程式的可重用性和可配置性。

- **Q4：**函式命名不具描述性
問題:
函式名稱 load_proc 和 c_res 缺乏語意，無法清楚表達其功能。
為什麼不好:
函式名稱應該清楚描述其行為，便於其他開發者理解程式碼。
不具描述性的命名會增加程式碼的學習成本。

- **Q5：** 缺乏錯誤處理
問題:
程式中對於可能的例外情況（如檔案不存在、檔案格式錯誤等）沒有進行適當的錯誤處理。
為什麼不好:
如果檔案格式不正確，程式會直接崩潰，導致使用者體驗不佳。
缺乏錯誤處理會使程式不夠健壯，無法應對非預期情況。

---

## 二、 Vibe-Coding 協作紀錄 (共6小題，每小題3分，總共18分)

請記錄你如何引導 GitHub Copilot 進行重構。
重構的要求，請根據題目卷以及你發現的 Code Smells 來進行。

### 2.1 關鍵 Prompt 紀錄

請貼出你對 AI 下達過最有效的 3 個 Prompt：

```log
Prompt A： 幫我找這個程式檔裏面的變數:l1、d2、c_res，告訴我他們的代表意義，並且告訴我建議修改成甚麼名稱

Prompt B： 請列出原程式碼中至少 5 個嚴重的設計問題，並說明為什麼不好

Prompt C：好，那現在請幫我照你說的，改l1、d2、c_res這三個變數的名稱，並且照你剛說的那幾個問題，修改我的程式碼
```

### 2.2 AI 錯誤處理

1. AI 在撰寫過程中是否有出錯？（例如：漏寫了某個功能、語法錯誤）你是如何發現並請它修正的？
有，我把你這三點丟給她，他自己幫我改程式碼

2. 發現的問題： (範例：AI 漏掉了 exit 時要印出分類統計的功能)
少了一個「結束前統計分類總額」的功能。也就是在 exit 前，應該要迴圈遍歷 library_data，計算每個狀態（例如：available、borrowed）有多少本書，並印出統計結果。
3. 修正的對話： (範例：我告訴它「請在結束前迴圈遍歷 JSON 資料並計算分類總額」)
請問你改這段程式碼的時候，有漏掉甚麼功能嗎?例如：漏寫了某個功能、語法錯誤，或是有漏掉了 exit 時要印出分類統計的功能，如果有的話，幫我更改程式碼讓他「請在結束前迴圈遍歷 JSON 資料並計算分類總額」

---

## 三、 成果驗證與測試  (共2小題，每小題10分，總共20分)

確保你的 `modern_library.py` 功能正確。

### 3.1 測試結果驗證 (Terminal)

請貼上執行程式並輸入 show 後，顯示資料清單。(請從呼叫執行程式到結束程式完整的把操作對話複製到下框)

```log

```

### 3.2 產出的資料檔案格式

請貼上重構後生成的 `books.json` 前幾行內容：

```json
[
  {
    "title": "XXX",
    "isbn": "XXX",
    "status": "XXX"
  }
]
```

---

## 四、 Git 提交紀錄 (Commit History)  (共2小題，總共13分)

### 4.1. 請執行 `git log --oneline` 並將 Commit 紀錄貼在此處，以證明你的開發流程(此小題6分)


```log
# 請在此處貼上 git log 內容
```

### 4.2. GitHub Repository 連結(此小題7分)

將專案推送到 GitHub (Public) 若提交到 Private 連結不給分。 請設定 Repository 名稱為 `學號-0527-Final` Ex: B11526748-0527-Final **請用 - 不是 _**

```log
# 請貼上你的 Repo 網址
```

希望這兩份 Markdown 格式能幫你省下不少排版時間！祝你的考試順利舉行！

最後請保留整份專案資料夾歐！
